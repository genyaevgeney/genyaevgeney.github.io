;(function($){
	"use strict";

	$(function(){
		$('.section__slider').slick({
			slidesToShow: 4,
			slidesToScroll: 1,
			// centerMode: true,
			// dots: true,
			// focusOnSelect: true
		});
	})
})(jQuery);